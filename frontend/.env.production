# Production API base URL for the deployed Spring Boot backend on Render.
# Vite automatically loads this file for production builds (npm run build).
# Local development (npm run dev) is unaffected and uses the Vite /api proxy -> http://localhost:8080
VITE_API_BASE_URL=https://keystone-backend-v2.onrender.com/api